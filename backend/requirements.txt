fastapi>=0.115.0
uvicorn[standard]>=0.34.0
pydantic>=2.0.0
# Redis (optional — falls back to in-memory if unavailable)
redis>=5.0.0
# Existing pipeline dependencies
yfinance>=0.2.0
pandas>=2.0.0
numpy>=1.24.0
scipy>=1.10.0
scikit-learn>=1.2.0
hmmlearn>=0.3.0
statsmodels>=0.14.0
# Testing
pytest>=8.0.0
httpx>=0.27.0
# Performance (optional — falls back gracefully)
orjson>=3.9.0
websockets>=12.0
# ML Pipeline (Phase 3–4)
celery>=5.3.0
xgboost>=2.0.0
pyarrow>=14.0.0
